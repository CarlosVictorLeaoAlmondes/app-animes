import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.exemple.app',
  appName: 'Registrando Animes',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
